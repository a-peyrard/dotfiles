#
# Auto-start devbridge server (Mac side — listens for devserver commands)
#

{
    if ! lsof -i :${DEVBRIDGE_MAC_PORT:-19998} -sTCP:LISTEN >/dev/null 2>&1; then
        nohup devbridge server >> /tmp/devbridge.log 2>&1 &
        disown
    fi
} &!
