#
# Auto-start devbridge server (devserver side — listens for Mac commands)
#

if ! pgrep -f "devbridge server" > /dev/null 2>&1; then
    nohup devbridge server >> /tmp/devbridge.log 2>&1 &
    disown
fi
