# Override system default (fbless.sh sets LESS="-n -i")
# -X = don't use alternate screen (pager output stays visible after exit)
# -R = pass raw ANSI color escapes (colored diffs, man pages)
export LESS="-n -i -X -R"
