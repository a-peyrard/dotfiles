# Zsh completion fpath setup
# Sets up completion paths before compinit runs (see 89_completions_init.env)

# custom completions
fpath=(~/.zsh_completions $fpath)

# private completions (work-specific tools, etc.)
if [[ -d ~/.zsh_completions.private ]]; then
    fpath=(~/.zsh_completions.private $fpath)
fi
